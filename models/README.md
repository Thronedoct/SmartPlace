# Model Weights

This directory is for local model weights and checkpoints.

Do not commit large weights. Record each weight file's source URL, download date, expected path, and checksum when available.

Recommended layout:

```text
models/
|-- opa/
|-- libcom/
`-- experiments/
```
